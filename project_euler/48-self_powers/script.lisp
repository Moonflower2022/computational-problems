; UNFINISHED

(defun power (current, index, a, b)
    "returns a^b"

    return-from power power()

)

(defun self_power_sum (current_sum, index, stop)
    return-from self_power_sum (self_power_sum (+ index (index))


)